# Node API Environment Variables

# Database connection string (required)
# Format: postgresql+asyncpg://username:password@host:port/database
# Note: Should be the SAME database as dashboard-api
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/waai_db

# CORS allowed origins (optional)
# Usually set to "*" to allow requests from any n8n instance
# Or specify your n8n domain: https://n8n.yourdomain.com
CORS_ORIGIN=*
